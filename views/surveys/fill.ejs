<!DOCTYPE html>
<html>
<head>
    <title>Fill Survey - Ella Rises</title>
    <link rel="stylesheet" href="/css/style.css">
</head>
<body class="dashboard-body">
    <%- include('../partials/navbar') %>

    <main class="page-container">
        <div class="page-header">
            <h1>Event Survey</h1>
            <p class="page-subtitle"><%= registration.event_name %></p>
        </div>

        <div class="form-card">
            <form action="/surveys/submit/<%= registration.registration_id %>" method="POST" class="crud-form">
                <div class="survey-question">
                    <label>How satisfied were you with this event? (1-5)</label>
                    <div class="rating-input">
                        <% for(let i = 1; i <= 5; i++) { %>
                            <input type="radio" name="survey_satisfaction_score" value="<%= i %>" id="sat<%= i %>" required>
                            <label for="sat<%= i %>"><%= i %></label>
                        <% } %>
                    </div>
                </div>

                <div class="survey-question">
                    <label>How useful was this event for you? (1-5)</label>
                    <div class="rating-input">
                        <% for(let i = 1; i <= 5; i++) { %>
                            <input type="radio" name="survey_usefulness_score" value="<%= i %>" id="use<%= i %>" required>
                            <label for="use<%= i %>"><%= i %></label>
                        <% } %>
                    </div>
                </div>

                <div class="survey-question">
                    <label>How would you rate the instructor? (1-5)</label>
                    <div class="rating-input">
                        <% for(let i = 1; i <= 5; i++) { %>
                            <input type="radio" name="survey_instructor_score" value="<%= i %>" id="inst<%= i %>" required>
                            <label for="inst<%= i %>"><%= i %></label>
                        <% } %>
                    </div>
                </div>

                <div class="survey-question">
                    <label>Overall rating (1-5)</label>
                    <div class="rating-input">
                        <% for(let i = 1; i <= 5; i++) { %>
                            <input type="radio" name="survey_overall_score" value="<%= i %>" id="overall<%= i %>" required>
                            <label for="overall<%= i %>"><%= i %></label>
                        <% } %>
                    </div>
                </div>

                <div class="form-group">
                    <label for="survey_comments">Additional Comments</label>
                    <textarea id="survey_comments" name="survey_comments" rows="4" placeholder="Share your thoughts..."></textarea>
                </div>

                <div class="form-actions">
                    <a href="/surveys" class="btn-cancel">Cancel</a>
                    <button type="submit" class="btn-submit">Submit Survey</button>
                </div>
            </form>
        </div>
    </main>

    <%- include('../partials/footer') %>
</body>
</html>

